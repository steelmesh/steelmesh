
/**
 * Module dependencies.
 */

var cluster = require('./')
  , http = require('http');

var server = http.createServer(function(req, res){
  res.end('test');
});

cluster(server)
  .set('workers', 4)
  .use(cluster.debug())
  .listen(3000, 'localhost');