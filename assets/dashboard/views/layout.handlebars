<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

	<link rel="stylesheet" href="/css/bootstrap.min.css">
	<link rel='stylesheet' href='/css/main.css'>
	<script src="/js/jquery-1.7.1.min.js"></script>
	
	<!-- 
    <link rel="shortcut icon" href="images/favicon.ico">
    <link rel="apple-touch-icon" href="images/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="images/apple-touch-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="114x114" href="images/apple-touch-icon-114x114.png">
	-->
  </head>

<body>
    <div class="topbar">
      <div class="fill">
        <div class="container">
          <a class="brand" href="/">{{ title }}</a>

			<ul class="nav">
			{{#each nav}}
				<li>
					{{#if items}}
					<a href="#" class="dropdown-toggle">{{ title }}</a>
					<ul class="dropdown-menu">
						{{#each items}}
						<li><a href="{{ url }}">{{ title }}</a></li>
						{{/each}}
					</ul>
					{{else}}
					<a href="{{ url }}">{{ title }}</a>
					{{/if}}
				</li>
			{{/each}}
			</ul>

		  <div class="pull-right sm-status">status:
			<span class="label {{#if online}}success{{/if}}{{#if shutdown}}important{{/if}}">{{ status }}</span>
		  </div>
          <!-- 
          <ul class="nav secondary-nav">
			<li class="dropdown">
				<a href="#" class="dropdown-toggle">Status: <span class="label">{{ status }}</span></a>
				<ul class="dropdown-menu">
			       <li><a href="#">Hi</a></li>
			    </ul>
			</li>
		  </ul>
		  -->
        </div>
      </div>
    </div>
	
    <div class="container">
		{{#each messages}}
		<div class="alert-message {{ type }}" data-alert="true">
			<a class="close" href="#">×</a>
			<p>{{ text }}</p>
		</div>
		{{/each}}

		{{{body}}}

	     <footer>
	       <p>&copy; Sidelab 2011</p>
	     </footer>

    </div> <!-- /container -->

	<script src="/js/bootstrap-alerts.js"></script>
	<script src="/js/bootstrap-modal.js"></script>
	<script src="/js/bootstrap-twipsy.js"></script>
	<script src="/js/bootstrap-popover.js"></script>
	<script src="/js/handlebars-1.0.0.beta.4.js"></script>
	<script src="/js/dash.js"></script>
  </body>
</html>
